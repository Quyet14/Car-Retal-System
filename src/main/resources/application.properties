spring.application.name=demo

# SQL Server Configuration
# Using localhost with dynamic port (works without SQL Browser)
spring.datasource.url=jdbc:sqlserver://localhost:56860;databaseName=Car-Retal-System;encrypt=true;trustServerCertificate=true;
spring.datasource.username=Quyet
spring.datasource.password=quyet146
spring.datasource.driver-class-name=com.microsoft.sqlserver.jdbc.SQLServerDriver

# JPA/Hibernate Configuration
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.SQLServerDialect
spring.jpa.properties.hibernate.format_sql=true

# H2 Console (disabled when using SQL Server)
spring.h2.console.enabled=false

# Mail Configuration (for development - console output)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false

# Application Configuration
server.port=9090
app.base-url=http://localhost:9090
